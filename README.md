# demiurge

Setup:

```shell
cabal sandbox init # je crois
cabal test # pour voir s'il marche
cabal run file # pour le faire courir (si jamais il ne peut pas marcher) où file est un fichier présent dans le dossier samples


brew install hlint # faut voir la commande mais cet outil est top
```
